# TODO List | 待办清单

## 正在做
- [ ] 策略模式实现，用于选择不同的key值的淘汰策略(LRU/LFU/FIFO) 和序列化方法(JSON/Protobuf ,用simdjson和flatbuffers主要用于持久化)
- [ ] SIMD加速哈希计算，get和exist和evict_key目前没加,set加了但这只处理第一个匹配的hash,会漏掉其他匹配的hash，需要一次性全部处理，另外需要测试性能是否提升,现在set,get,exist里面加了avx2,需要封装出一个普通的get,set,exist,evict_key,用于测试性能，并需要一个可以测出差距的场景，不能太简单

## 待实现
- [ ] 目前服务端其实还是单线程，并没有开启多线程，需要设计实现多线程服务端
- [ ] 目前协议太简单，需要实现RESP协议
- [ ] 实现RDMA服务端和客户端
- [ ] 服务端加入SIMD向量化优化
- [ ] 增加实现RDMA服务端和客户端，并提供C++层调用
- [ ] 实现消息分发功能，同时支持tcp和rdma
- [ ] 用nanobind实现python层封装，提供python层调用
- [ ] 装饰器模式，加功能不改动核心代码，如加过期，加权限，统计QPS等
- [ ] 完善测试用例,包括功能测试和性能测试，特别是多线程场景
- [ ] 设计分布式架构，主从复制，分布式事务，grpc接口，心跳检测，故障切换，负载均衡，共识算法(用Raft算法，用策略模式给其他算法留接口)

## 待优化
- [ ] 观察者模式用CAS替换锁,目前锁会严重影响性能
- [ ] 优化哈希函数，让key尽可能分布均匀，设置合理的capacity_等参数
- [ ] 服务端和客户端日志输出优化
- [ ] RDMA减少内存拷贝，实现零拷贝，考虑使用NUMA
- [ ] 适配器模式，兼容不同的底层结构，如跳表，B树等
- [ ] 加入c++20新特性，如span，span_view和协程库等，优化内存使用

## BUG 修复
- [x] HashMap set,找到桶后需要检查key是否存在，如果存在要覆盖原有的key，现有的代码不会检查而是直接插入一个新节点，会浪费内存和哈希性能也会越来越差
- [x] HashNode的key是引用类型，会导致传入key如果是个临时变量，会导致悬空引用问题，需要修改为值类型
- [x] 修复多个客户端同时连接时卡住问题,问题在于epoll边缘触发时需要循环read直到EAGAIN

## 已完成 ✅
- [x] TCP服务端的客户端buffer太小，调大之后发现流式解析逻辑有问题，需要修复,buffer_size目前设置为1MB,服务端设置为读到EAGAIN再处理客户端请求，而是一读到数据就直接处理，客户端逻辑也修改了，用一个string循环用4KB的buffer接收响应，而不是写死只接收4KB的数据
- [x] 链式哈希改为开发寻址哈希,内存更连续CPU缓存命令率高,没有指针跳转性能开销,用开放哈希寻址能用SIMD进行加速,无锁设计和扩容更简单，链式哈希需要频繁分配内存，防止内存碎片而设计内存池，而开放寻址哈希是连续内存不用
- [x] 需要配置的东西慢慢多起来了，需要用config.yml统一配置
- [x] 观察者模式实现，用于通知key值的变化(用于淘汰，过期删除，主从同步和分布式事务)
- [x] Server也需要用工厂模式封装，支持tcp和rdma
- [x] TCP 服务端
- [x] TCP 客户端封装为lib,提供C++层调用
- [x] 工厂模式封装 TCP/RDMA
- [x] SET / GET 接口
- [x] MIT LICENSE
- [x] 无锁哈希表，无锁内存池存取
- [x] KV分片存储引擎